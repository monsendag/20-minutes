/** Wall-clock length of every session. */
export const SESSION_DURATION_MS = 20 * 60 * 1000;

/** Soft cap so a rep stays at least one second long. */
export const MAX_REPS = 1200;

export const DB_NAME = 'idb://twenty-minutes';
